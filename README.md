# hanglisimonwang-kb

Public agent-onboarding knowledge base for Simon Wang's `LihangSimonCollab` research workspace.

Live page: <https://tesolchina.github.io/hanglisimonwang-kb/>

This repo holds **only** non-confidential structural information so that other AI agents (Cursor, Claude Code, Codex CLI, etc.) can read it before being pointed at the private codebase. The actual code lives in the private repo `tesolchina/LihangSimonCollab` — request a token from Simon to access it.

## Files
- `index.html` — the onboarding page (self-contained, no build step)

## Updating
Edit `index.html` directly and push. GitHub Pages serves from the `main` branch root.
